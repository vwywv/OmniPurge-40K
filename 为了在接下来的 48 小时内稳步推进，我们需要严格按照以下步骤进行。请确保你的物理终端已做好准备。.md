为了在接下来的 48 小时内稳步推进，我们需要严格按照以下步骤进行。请确保你的物理终端已做好准备。

#### 步骤 0：铸造厂环境准备 (Environment & Setup)

1. **确认基础环境**：

   - 安装 **Python 3.10+**（我们的主控脚本语言）。
   - 确认已安装 **Java JDK**，在终端运行 `javac -version` 确保可用（本地审判庭依赖）。

2. **安装 Python 依赖（极其轻量）**：

   ```
   bash
   pip install requests rich
   ```

   (仅此两个即可。`requests` 用于调 API，`rich` 用于终端特效。我们将不再需要 pylint 等重型工具)

3. **创建核心项目目录**：

   ```
   bash
   mkdir -p omnipurge-40k/src
   touch omnipurge-40k/src/main.py   # 主入口
   touch omnipurge-40k/src/mock.py   # 用于前期断网测试的假数据
   ```

#### 步骤 1：第一突击阶段 —— 亵渎捕获与逆向寻根 (约 1-2 小时)

*目标：跑通 `cat error.log | python main.py`，能精准截取出报错 Java 文件的上下文。*

1. **防呆监听流**：在 `main.py` 写入 `select` 模块，监听 `sys.stdin` 3秒，无输入直接打印红色警告退出。
2. **正则嗅探**：编写正则表达式 `at\s+[\w\.]+\(([\w]+\.java):(\d+)\)`，从输入流中抓取 Java 文件名和行号。
3. **启发式倒推 (Heuristic Backtracking)**：编写一个循环，用 `os.path.exists()` 沿着当前工作目录向下寻找这个 `.java` 文件，过滤掉 Spring/JDK 的幽灵堆栈，锁定真实的业务代码文件。
4. **上下文截取**：读取锁定的 `.java` 文件，以报错行号为基准，截取上下 20 行，组装成文本。